# Tasks

1. Verify current repo separation and keep `infinite-architecture` out of scope.
2. Add the browser config bridge for the frontend API base URL.
3. Add the static build output and Vercel config.
4. Extend the backend provider routing for Groq, DeepSeek, and Hermes.
5. Validate the backend health, config, Groq response, and DeepSeek fallback.
6. Document rollback and deployment assumptions in OpenSpec.
7. Deploy the frontend to Vercel with the backend URL wired in.
8. Deploy the backend to the VPS and confirm smoke checks.
